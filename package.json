{
  "name": "stadli-minimal-cf",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --local",
    "deploy": "wrangler deploy",
    "format": "prettier -w .",
    "typecheck": "tsc --noEmit",
    "cf:init": "node scripts/init-resources.js",
    "test": "node tests/smoke.js"
  },
  "dependencies": {
    "eta": "^3.4.0"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "wrangler": "^3.80.0",
    "prettier": "^3.3.3",
    "@cloudflare/workers-types": "^4.20241022.0"
  }
}